{
  "name": "@bimdive/rest-api-client",
  "version": "1.0.9",
  "main": "dist/index.js",
  "license": "MIT",
  "scripts": {
    "build": "rm -rf src dist && API_URI=https://rest.bimdive.com ./generate.sh && npx tsc && yarn version --patch --no-git-tag-version"
  },
  "devDependencies": {
    "@types/request": "^2.48.5",
    "typescript": "^4.0.3"
  },
  "dependencies": {
    "node-fetch": "^2.6.1"
  }
}
